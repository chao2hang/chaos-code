# chaos-code-linux-x64

[`chaos-code`](https://www.npmjs.com/package/chaos-code) 在 linux-x64 上的平台专用二进制包。

请勿直接安装本包，请安装主包：

```sh
npm install -g chaos-code
```

主包会通过 `optionalDependencies` 自动拉取匹配你所在平台的二进制文件。
